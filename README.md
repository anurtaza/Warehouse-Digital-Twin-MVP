# 🏭 Warehouse Digital Twin — MVP

3D цифровой двойник склада на Python + React + Three.js.

## Что внутри

```
warehouse-twin/
├── backend/
│   ├── app.py           # Flask + SocketIO сервер
│   ├── simulator.py     # Движок симуляции склада и WMS-имитация
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/
│   ├── src/
│   │   ├── App.jsx                    # Главный компонент + Three.js сцена
│   │   ├── App.css
│   │   └── components/
│   │       ├── MetricsBar.jsx         # Метрики в реальном времени
│   │       ├── ScenarioPanel.jsx      # Панель выбора сценариев
│   │       ├── ThroughputChart.jsx    # График пропускной способности
│   │       ├── EventLog.jsx           # Лог событий
│   │       ├── InsightsPanel.jsx      # Аналитика и рекомендации
│   │       ├── WmsPanel.jsx           # Сводка WMS-инвентаря
│   │       ├── RolePanel.jsx          # Выбор пользовательской роли
│   │       ├── RoleSummaryPanel.jsx   # Роль и бизнес-показатели
│   │       ├── ScenarioAnalysisPanel.jsx # Сценарный анализ
│   │       └── ReportPanel.jsx        # Экспорт отчёта
│   ├── index.html
│   ├── vite.config.js
│   ├── package.json
│   ├── Dockerfile
│   └── nginx.conf
└── docker-compose.yml
```

## Быстрый старт

### Вариант 1 — Docker (рекомендуется)

```bash
docker compose up --build
```

Открыть: http://localhost:3000

### Вариант 2 — Вручную

**Бэкенд:**
```bash
cd backend
pip install -r requirements.txt
python app.py
# Сервер: http://localhost:5000
```

**Фронтенд (в другом терминале):**
```bash
cd frontend
npm install
npm run dev
# UI: http://localhost:3000
```

## Функциональность

### 3D-сцена склада
- Интерактивная визуализация всей карты склада 10×6.
- Ячейки хранилища с динамическим состоянием: заполнена, свободна, горячая зона.
- AGV с визуальным маршрутом и целевой точкой.
- Открытый план склада без лишних перегородок — только стеллажи и коридоры.
- Подсказки по ячейкам и роботам при наведении.

### Маршрутизация AGV
- AGV выбирают цель по всей территории склада.
- Строятся маршруты через ближайший коридор, затем по осям X/Z, и только потом вход в целевую ячейку.
- Визуализация траектории AGV в виде пунктирной линии.
- Плавное движение и корректный поворот AGV по курсу.

### Панели и аналитика
- Режимы пользователя: менеджер склада, оператор, логист.
- Панель метрик в реальном времени: заполненность, активные AGV, заказы, скорость обработки.
- Сценарии: `normal`, `surge`, `agv_fail`, `low_staff`.
- Анализ сценариев и прогнозы производительности.
- WMS-панель с топ-SKU, инвентарем и очередью заказов.
- Экспорт отчёта в JSON.

### Симуляция склада и WMS
- Движок имитирует заполнение и опорожнение ячеек.
- Генерация заказов, смена статусов, синхронизация инвентаря.
- AGV-логика с выбором дальних целей и стабильной сменой маршрута.
- Ручной режим оптимизации размещения SKU по приоритету и расстоянию.
- Система событий с информационными и предупреждающими сообщениями.

## API

| Метод | Эндпоинт | Описание |
|-------|----------|----------|
| GET | `/api/state` | Полное состояние склада |
| GET | `/api/metrics` | Только метрики |
| GET | `/api/events` | Лог событий |
| POST | `/api/scenario/{name}` | Переключить сценарий |
| POST | `/api/optimize` | Оптимизировать размещение SKU |

**Сценарии:** `normal`, `surge`, `agv_fail`, `low_staff`

**WebSocket события:**
- `state` — полное обновление состояния
- `event` — новое событие в логе

## Подключение реального WMS

Замените `simulator.py` на коннектор к вашей системе:

```python
class WarehouseSimulator:
    def get_state(self):
        # Вместо симуляции — запрос к WMS API:
        return wms_client.fetch_current_state()

    def tick(self):
        # Polling реальных данных
        pass
```

Поддерживаемые протоколы подключения:
- REST API WMS (SAP EWM, 1C WMS, Manhattan)
- MQTT/OPC-UA от сенсоров и RFID
- PostgreSQL / ClickHouse для исторических данных

## Стек

| Слой | Технология |
|------|-----------|
| 3D рендер | Three.js (OrbitControls) |
| Фронтенд | React 18 + Vite |
| Реальное время | Socket.IO (WebSocket) |
| Бэкенд | Python Flask + Flask-SocketIO |
| Деплой | Docker Compose + Nginx |

## Что улучшено в MVP

- Полностью обновлённый 3D-вид склада с открытым планом и четкой навигацией.
- Коридорно-ориентированная маршрутизация AGV.
- Панели ролей, WMS, сценарного анализа и отчётов.
- Экспорт отчёта и расширенные бизнес-метрики.
- Улучшенная симуляция AGV с дальними целями и стабильной сменой маршрутов.
- Автоматическая потоковая передача состояния через WebSocket.
